# Toolchain

We use [invoke](https://www.pyinvoke.org/) for simple build automation.

Evaluated alternatives:
- snakemake (good, heavy, not available in customer's environment)
- pybuilder (good, not available in customer's environment)
- plain old make (minimal, no dependency on python, requires separate installation) 