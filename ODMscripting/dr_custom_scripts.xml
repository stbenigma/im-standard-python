<?xml version="1.0" encoding="UTF-8" ?>
<dr_custom_scripts>
	<scr id="E60A5A28-BB9B-3787-10E7-259DF900B9E6" name="Table abbreviation to column" object="relational" engine="Oracle Nashorn" type="" var="model" library="" method="" purpose="transformation" >
		<script>
			<![CDATA[// variable used to return custom message
var ruleMessage;
// variable used to return the type of the rule Error
// or Warning and red/blue color will be used for result
// if another custom type is returned (Problem in the rule below) 
// then result is colored in yellow
var errType;
var table;
function checkcomments(object){
 result = true;
 ruleMessage="";
 if(table.getCommentInRDBMS().equals("")){
  ruleMessage="no comments in RDBMS defined";
  errType="Problem:";
  result = false;
 }
 if(table.getComment().equals("")){
  if(ruleMessage.equals("")){
   ruleMessage="no comments defined";
  }else{
   ruleMessage= ruleMessage +" , no comments defined";
   }
   // can be changed to Warning    
   errType="Error";
  return false;
 }
 return result;
}
//rule for not null constraint name
function NotNullConstr_Name_Length(column){
 model = column.getDesignPart();
 name = column.getNotNullConstraintName();
 result = true;
 if(name!=null && !"".equals(name)){
	holder = model.getNamingOptionsHolderFor("oracle.dbtools.crest.model.design.constraint.TableLevelConstraint");
	if ((holder.getMaxNameLength() > 0) && (name.length() > holder.getMaxNameLength())) {
		result = false;
	}
 }
 return result
}]]>
		</script>
	</scr>
	<scr id="0528C35C-F29B-E7BB-57AC-37BA2780A98D" name="Table template - uses column name" object="relational" engine="Oracle Nashorn" type="" var="model" library="" method="" purpose="transformation" >
		<script>
			<![CDATA[// columns are found by column name
// allowing reuse of already existing columns
// dynamic property ctemplateID is set afterwards - will keep connection to template
// column even if the name of column is changed
var t_name = "table_template";
var p_name = "ctemplateID";
template = model.getTableSet().getByName(t_name);
if(template!=null){
    tcolumns = template.getElements();
    tables = model.getTableSet().toArray();
    for (var t = 0; t<tables.length;t++){
     table = tables[t];
    // compare name ignoring the case
     if(!table.getName().equalsIgnoreCase(t_name)){
         for (var i = 0; i < tcolumns.length; i++) {
            column = tcolumns[i];
            col = table.getElementByName(column.getName());
            if(col==null){
             col = table.getColumnByProperty(p_name,column.getObjectID());
            }
            if(col==null){
             col = table.createColumn();
            }
            column.copy(col);
            //set property after copy otherwise it'll be cleared by copy
            col.setProperty(p_name,column.getObjectID());
            table.setDirty(true);
         }
     }
    }
}]]>
		</script>
	</scr>
	<scr id="F54B1A9C-F951-EB88-E905-5599533569B1" name="Create subview from tables in serch result " object="relational" engine="Oracle Nashorn" type="" var="model" library="" method="" purpose="transformation" >
		<script>
			<![CDATA[objs = model.getLastSearchResult().toArray();
var dp;
var sv;
//create subview and add tables to it
for(var i = 0;i<objs.length;i++){
	obj = objs[i];
	// use "Entity" for entities and logical model
	if(obj.getObjectTypeName().equals("Table")){
		if(dp==null){
			dp = obj.getDesignPart();
			sv = dp.createDesignPartSubView();
			// uncomment next line if want to set name
		     //sv.setName("Name");
		     sv.getPlaceHolder().setVisible(true);
		}
		sv.addViewFor(obj);
	}
}
if(dp!=null){
 //add foreign keys if you want to
 for(var i = 0;i<objs.length;i++){
	obj = objs[i];
	// use "Entity" for entities and logical model
	if(obj.getObjectTypeName().equals("Table")){
		tv = obj.getFirstViewForDPV(sv);
		if(tv!=null){
			//for entities and logical model use 
			//tv.addTVRelations(); 
			tv.addTVFKRelations();
		}
	}
 }
 //add Arcs if you want to
 for(var i = 0;i<objs.length;i++){
	obj = objs[i];
	if(obj.getObjectTypeName().equals("Table")){
		tv = obj.getFirstViewForDPV(sv);
		if(tv!=null){
			tv.addArcs();
		}
	}
 }
 //arrange diagram
 sv.rearrangeNewDiagram();
}]]>
		</script>
	</scr>
	<scr id="E8592135-C3C6-7E71-D837-14B6B3C1D6DC" name="replace non oracle characters" object="relational" engine="Oracle Nashorn" type="" var="model" library="translation" method="initTranslation" purpose="transformation" >
		<script>
			<![CDATA[var gLogNote = "LOG";
var gCurrentLang = "DE";
var gLanguages = "DE,EN";
var gTranslInforegexp =/TRANSLATION\{[^}]*\}/;
var logInitialized = false;

function initLog(){
	if (! logInitialized){
		var lNote = model.getNoteSet().getByName(gLogNote);
     	if (lNote == null) {
    	    		lNote = model.createNote()
	    		lNote.setName(gLogNote);
     	}
     	lNote.setComment("");
     	logInitialized = true;
	}
}
function log(plog){
	initLog();
	var lNote = model.getNoteSet().getByName(gLogNote);
     lNote.setComment(lNote.getComment()+"\n"+plog);
}

function getTranslInfo(){
	var lDesignComment = model.design.getComment();
     lmatchTransl= lDesignComment.match(gTranslInforegexp);
     return lmatchTransl;    
}

function setTranslInfo(curLang,langs) {
	model.design.setComment(
		"TRANSLATION{\ncurrentLang="
		+curLang+"\nlanguages="
		+langs+"\nlastSync=\n}\n"
		+ model.design.getComment());
}
function updTranslInfo(curLang,langs,datetime) {
	curComment = model.design.getComment();
	model.design.setComment(
		curComment.replace(gTranslInforegexp,
		"TRANSLATION{\ncurrentLang="
		+curLang+"\nlanguages="
		+langs+"\nlastSync="+datetime+"\n}"
		));
}

function textFromNote(lang,obj) {
	return obj.getNotes()
}

function textToNote(lang,obj,value) {
	obj.setNotes(value)
}

function init(){
	lmatchTransl = getTranslInfo();
	if (lmatchTransl == null) {
		throw new Error("***TRANSLATION not installed***");
	}
	ltext= String(lmatchTransl).match(/currentLang=[A-Z]{2}/);
	lparts = String(ltext).split("=");
	//log("curLang="+lparts[1]);
     gCurrentLang = lparts[1];

	ltext= String(lmatchTransl).match(/languages=[A-Z,]*/);
	lparts = String(ltext).split("=");
     //log("languages="+lparts[1]);
     gLanguages = lparts[1]
}

function doProperty(lang,propName,obj,value){
	lpropName = lang + propName;
	if (lang == gCurrentLang) {
		// die Werte kommen aus dem Objekt, überschreibe immer
		obj.setProperty(lpropName
		      	 	,value)
	} else {
	   	ldest = obj.getProperty(lpropName);
	   	// überschreibe nur, wenn das Ziel leer ist
		if (ldest == '')  {
		 	// bei leerem sourceWert setze keinen Prefix
			if (value != '') {
				lprefix =  '*'+gCurrentLang+'* '
				obj.setProperty(lpropName,lprefix+value)
			} 
			
		}
	}
}

function doNote(lang,obj,value) {
	startStr = '['+lang+'['
	endStr = ']'+lang+']'
	if (lang == gCurrentLang) {
		// die Werte kommen aus dem Objekt, überschreibe immer
		textToNote(lang,obj,value)
	} else {
	   	ldest = textFromNote(lang,obj);
	   	// überschreibe nur, wenn das Ziel leer ist
		if (ldest == '')  {
		 	// bei leerem sourceWert setze keinen Prefix
			if (value != '') {
				lprefix =  '*'+gCurrentLang+'* '
				textToNote(lang,obj,lprefix+value)
			} 
			
		}
	}
	
}			
	
function fuellUDP(){
	entities = model.getEntitySet().toArray();
	for (i=0;i<entities.length;i++) { //
		ent = entities[i];
		// noch nicht initialisiert
		doProperty("DE","_ENTI_NAME",ent,ent.getName())
//		doProperty("DE","_ENTI_COMMENT",ent,ent.getComment())
		doNote("DE",ent,ent.getComment())
		doProperty("DE","_ENTI_SYNONYM",ent,ent.getSynonym())
		doProperty("EN","_ENTI_NAME",ent,ent.getName())
//		doProperty("EN","_ENTI_COMMENT",ent,ent.getComment())
		doNote("EN",ent,ent.getComment())
		doProperty("EN","_ENTI_SYNONYM",ent,ent.getSynonym())
	
		attrs = ent.getAttributes().toArray();
		//log("Attrs="+attrs.length)
		for (j=0;j<attrs.length;j++) { //
			attr = attrs[j];
			//log(attr.getName(),attr.getComment());
			doProperty("DE","_ATTR_NAME",attr,attr.getName());
//			doProperty("DE","_ATTR_COMMENT",attr,attr.getComment());			
			doNote("DE",attr,attr.getComment());			
			doProperty("EN","_ATTR_NAME",attr,attr.getName());
//			doProperty("EN","_ATTR_COMMENT",attr,attr.getComment());			
			doNote("EN",attr,attr.getComment());			
		}
		ent.setDirty(true);
	}
	relationships = model.getRelationSet().toArray();
	for (r=0;r<relationships.length;r++) { //
		rel = relationships[r];
		sourcename = rel.getNameOnSource()
		targetname = rel.getNameOnTarget()
		doProperty("DE","_RELA_TEXT_FROM",rel,sourcename)
		doProperty("DE","_RELA_TEXT_TO",rel,targetname)
		doProperty("EN","_RELA_TEXT_FROM",rel,sourcename)
		doProperty("EN","_RELA_TEXT_TO",rel,targetname)
		rel.setDirty(true);
	}
}

function setProperties(lang){
	entities = model.getEntitySet().toArray();
	for (i=0;i<entities.length;i++) { //
		ent = entities[i];
		ent.setName(ent.getProperty(lang+"_ENTI_NAME"));
//		ent.setComment(ent.getProperty(lang+"_ENTI_COMMENT"));
		ent.setComment(textFromNote(lang,ent))
		ent.setSynonym(ent.getProperty(lang+"_ENTI_SYNONYM"));
		
		attrs = ent.getAttributes().toArray();
		//log("Attrs="+attrs.length)
		for (j=0;j<attrs.length;j++) { //
			attr = attrs[j];
			//log(attr.getName(),attr.getComment());
			attr.setName(attr.getProperty(lang+"_ATTR_NAME"));
//			attr.setComment(attr.getProperty(lang+"_ATTR_COMMENT"));			
			attr.setComment(textFromNote(lang,attr))
		}		
		ent.setDirty(true);
	}
	relationships = model.getRelationSet().toArray();
	for (r=0;r<relationships.length;r++) { 
		rel = relationships[r];
		rel.setNameOnSource(rel.getProperty(lang+"_RELA_TEXT_FROM"));
		rel.setNameOnTarget(rel.getProperty(lang+"_RELA_TEXT_TO"));
		rel.setDirty(true);
	}
}

function curDateStr(){
	var currentdate = new Date();
	return  currentdate.getDate() 
	+ "/" + (currentdate.getMonth()+1) 
	+ "/" + currentdate.getFullYear() + "  " 
	+ currentdate.getHours() + ":" 
	+ currentdate.getMinutes() + ":" + currentdate.getSeconds();
}
function initTranslation() {
	lmatchTransl= getTranslInfo()
	if (lmatchTransl == null) {
		setTranslInfo(gCurrentLang
		,gLanguages);		
	} else {
		throw new Error("***TRANSLATION already installed***");
	}
	//hier gibt es einen Eintrag für die Translation
	init();
	// fülle die UDP für die Übersetzungen
	fuellUDP();
	updTranslInfo(gCurrentLang, gLanguages
				,curDateStr());
	model.design.setDirty(true);
}


function changeLang(pfromLang,ptoLang){
	//log("3:"+gLanguages);
//	log("change from " + pfromLang + " to " + ptoLang);
	fuellUDP();
	setProperties(ptoLang);
	gCurrentLang = ptoLang;
	updTranslInfo(gCurrentLang, gLanguages
				,curDateStr());	
	model.design.setDirty(true);
}

function change2DE() {
	init();
	if (gCurrentLang != 'DE') {
		changeLang(gCurrentLang,'DE');
	}
}
	
function change2EN() {
	init();
	if (gCurrentLang != 'EN') {
		changeLang(gCurrentLang,'EN');
	}
}]]>
		</script>
	</scr>
	<scr id="E02D7BAD-4F69-9925-05D0-8198BDC93138" name="TRANSL_initTranslation" object="logical" engine="Oracle Nashorn" type="" var="model" library="translation" method="initTranslation" purpose="transformation" >
		<script>
			<![CDATA[var gLogNote = "LOG";
var gCurrentLang = "DE";
var gLanguages = "DE,EN";
var gTranslInforegexp =/TRANSLATION\{[^}]*\}/;
var logInitialized = false;

function initLog(){
	if (! logInitialized){
		var lNote = model.getNoteSet().getByName(gLogNote);
     	if (lNote == null) {
    	    		lNote = model.createNote()
	    		lNote.setName(gLogNote);
     	}
     	lNote.setComment("");
     	logInitialized = true;
	}
}
function log(plog){
	initLog();
	var lNote = model.getNoteSet().getByName(gLogNote);
     lNote.setComment(lNote.getComment()+"\n"+plog);
}

function getTranslInfo(){
	var lDesignComment = model.design.getComment();
     lmatchTransl= lDesignComment.match(gTranslInforegexp);
     return lmatchTransl;    
}

function setTranslInfo(curLang,langs) {
	model.design.setComment(
		"TRANSLATION{\ncurrentLang="
		+curLang+"\nlanguages="
		+langs+"\nlastSync=\n}\n"
		+ model.design.getComment());
}
function updTranslInfo(curLang,langs,datetime) {
	curComment = model.design.getComment();
	model.design.setComment(
		curComment.replace(gTranslInforegexp,
		"TRANSLATION{\ncurrentLang="
		+curLang+"\nlanguages="
		+langs+"\nlastSync="+datetime+"\n}"
		));
}

function init(){
	lmatchTransl = getTranslInfo();
	if (lmatchTransl == null) {
		throw new Error("***TRANSLATION not initstalled***");
	}
	ltext= String(lmatchTransl).match(/currentLang=[A-Z]{2}/);
	lparts = String(ltext).split("=");
	//log("curLang="+lparts[1]);
     gCurrentLang = lparts[1];

	ltext= String(lmatchTransl).match(/languages=[A-Z,]*/);
	lparts = String(ltext).split("=");
     //log("languages="+lparts[1]);
     gLanguages = lparts[1]
}

function doProperty(lang,propName,obj,value){
	lpropName = lang + propName;
	if (lang == gCurrentLang) {
		// die Werte sind ok überschreibe immer
		obj.setProperty(lpropName
		       ,value)
	} else {
		ldest = obj.getProperty(lpropName);
		//log("ldest="+ldest+" value="+value);
		// schreibe nur, wenn Ziel leer ist.
		if (ldest == '')  {
		//   log("2ldest="+ldest+" value="+value);		  
		 	// bei leerem Wert setze keinen Prefix
			lprefix = (value == '') ? '' 
				: '*'+gCurrentLang+'* '
			obj.setProperty(lpropName	
	   	            ,lprefix+value)
		}

	}
}
	
function fuellUDP(lang){
	entities = model.getEntitySet().toArray();
//	ent = entities.getByName('Agent');
//	log("Entitäten="+entities.length);
	for (i=0;i<entities.length;i++) { //
		ent = entities[i];
		doProperty(lang,"_ENTI_NAME",ent,ent.getName())
		doProperty(lang,"_ENTI_COMMENT",ent,ent.getComment())
		doProperty(lang,"_ENTI_SYNONYM",ent,ent.getSynonym())
		
		attrs = ent.getAttributes().toArray();
		//log("Attrs="+attrs.length)
		for (j=0;j<attrs.length;j++) { //
			attr = attrs[j];
			//log(attr.getName(),attr.getComment());
			doProperty(lang,"_ATTR_NAME",attr,attr.getName());
			doProperty(lang,"_ATTR_COMMENT",attr,attr.getComment());			
		}		
		ent.setDirty(true);
	}
	relationships = model.getRelationSet().toArray();
	for (r=0;r<relationships.length;r++) { //
		rel = relationships[r];
		doProperty(lang,"_RELA_TEXT_FROM",rel,rel.getNameOnSource())
		doProperty(lang,"_RELA_TEXT_TO",rel,rel.getNameOnTarget())
		rel.setDirty(true);
	}
}

function liesUDP(lang){
	entities = model.getEntitySet().toArray();
	for (i=0;i<entities.length;i++) { //
		ent = entities[i];
		ent.setName(ent.getProperty(lang+"_ENTI_NAME"));
		ent.setComment(ent.getProperty(lang+"_ENTI_COMMENT"));
		ent.setSynonym(ent.getProperty(lang+"_ENTI_SYNONYM"));
		
		attrs = ent.getAttributes().toArray();
		//log("Attrs="+attrs.length)
		for (j=0;j<attrs.length;j++) { //
			attr = attrs[j];
			//log(attr.getName(),attr.getComment());
			attr.setName(attr.getProperty(lang+"_ATTR_NAME"));
			attr.setComment(attr.getProperty(lang+"_ATTR_COMMENT"));			
		}		
		ent.setDirty(true);
	}
	relationships = model.getRelationSet().toArray();
	for (r=0;r<relationships.length;r++) { 
		rel = relationships[r];
		if (rel.getName()=='Relation_42'){
		//	log(lang+"textfrom="+rel.getProperty(lang+"_RELA_TEXT_FROM"));
			rel.setComment("from="+rel.getProperty(lang+"_RELA_TEXT_FROM")
			+"\nto="+rel.getProperty(lang+"_RELA_TEXT_TO"))
		}
		rel.setNameOnSource(rel.getProperty(lang+"_RELA_TEXT_FROM"));
		rel.setNameOnTarget(rel.getProperty(lang+"_RELA_TEXT_TO"));
		rel.setDirty(true);
	}
}

function curDateStr(){
	var currentdate = new Date();
	return  currentdate.getDate() 
	+ "/" + (currentdate.getMonth()+1) 
	+ "/" + currentdate.getFullYear() + "  " 
	+ currentdate.getHours() + ":" 
	+ currentdate.getMinutes() + ":" + currentdate.getSeconds();
}
function initTranslation() {
	lmatchTransl= getTranslInfo()
	if (lmatchTransl == null) {
		setTranslInfo(gCurrentLang
		,gLanguages);		
	} 
	//hier gibt es einen Eintrag für die Translation
	init();
	// fülle die UDP für die Übersetzungen
	fuellUDP('DE');
	fuellUDP('EN');
	updTranslInfo(gCurrentLang, gLanguages
				,curDateStr());
	model.design.setDirty(true);
}


function changeLang(pfromLang,ptoLang){
	//log("3:"+gLanguages);
//	log("change from " + pfromLang + " to " + ptoLang);
	fuellUDP(pfromLang);
	liesUDP(ptoLang);
	gCurrentLang = ptoLang;
	updTranslInfo(gCurrentLang, gLanguages
				,curDateStr());	
}

function change2DE() {
	init();
	changeLang(gCurrentLang,'DE');
}
	
function change2EN() {
	init();
	changeLang(gCurrentLang,'EN');
}]]>
		</script>
	</scr>
	<scr id="41361C9B-AE29-7641-972C-945450BB790E" name="dummy" object="logical" engine="Oracle Nashorn" type="" var="model" library="" method="" purpose="transformation" >
		<script>
			<![CDATA[ents = model.getEntitySet().toArray();
ent = ents[1];
ent2 = model.getEntitySet().getByName('TEST');
//var path="/Users/stb/Documents/Projekte/FYAYC_intern/gitHub/translation/translation.udposdm";
 var path="/Users/stb/Documents/Projekte/EZV/gitHub/IM/EZV_Stammdaten/files/EZVProperties.udposdm";
 var Paths = Java.type('java.nio.file.Paths');
var Files = Java.type('java.nio.file.Files');
var lines = Files.readAllLines(Paths.get(path), Java.type('java.nio.charset.StandardCharsets').UTF_8);
var entity = [];
var attr =[]; 
var rels =[]; 
//lines.forEach(function(line) { data.push(line); });
 lines.forEach(function(line) { 
	//throw new Error ("line="+line);
	f = line.match('<property name="([A-Za-z_-]+)"');
	if (f != null ) {propName = f[1]}
	//throw new Error ("propname="+propName);
	t = line.match('design.logical.([^"]*)"');
	if (t != null) {		
		if (t[1].match('Attribute')) {attr.push(propName);}
		if (t[1].match('Relation')) {rels.push(propName);}
		if (t[1].match('Entity')) {entity.push(propName);}
	}
	});
//	i = entity.indexOf('DE_ENTI_SYNONYM');
//	if (i > -1) 	throw new Error (i+'DE_ENTI_SYNONYM')
	throw new Error (attr.length.toString()+":"+entity.length.toString()+":"+rels.length.toString());]]>
		</script>
	</scr>
	<scr id="4B7E11A4-7791-9090-5842-A5425B4D53A1" name="TRANSL_change2DE" object="logical" engine="Oracle Nashorn" type="" var="model" library="translation" method="change2DE" purpose="transformation" >
		<script>
			<![CDATA[//scripts to handle translation of modelx
var gLogNote = "LOG";
var gCurrentLang = "DE";
var gLanguages = "DE,EN";
var gTranslInforegexp =/TRANSLATION\{[^}]*\}/;
var logInitialized = false;
var notesStartTag = '[<tag>[\n';
var notesEndTag = '\n]<tag>]';
var plainMessage = 1;
var errorMessage = 0;

function fullmessage(msg,type) {
	Java.type("javax.swing.JOptionPane").showMessageDialog(null
	, msg,'ODM-Translation', type);
}

function error(msg) {
	fullmessage(msg,errorMessage);
	}
function message(msg) {
	fullmessage(msg,plainMessage);
	}

function initLog(){
	if (! logInitialized){
		var lNote = model.getNoteSet().getByName(gLogNote);
     	if (lNote == null) {
    	    		lNote = model.createNote()
	    		lNote.setName(gLogNote);
     	}
     	lNote.setComment("");
     	logInitialized = true;
	}
}
function log(plog){
	initLog();
	var lNote = model.getNoteSet().getByName(gLogNote);
     lNote.setComment(lNote.getComment()+"\n"+plog);
}

function getTranslInfo(){
	var lDesignComment = model.design.getComment();
     lmatchTransl= lDesignComment.match(gTranslInforegexp);
     return lmatchTransl;    
}

function setTranslInfo(curLang,langs) {
	model.design.setComment(
		"TRANSLATION{\ncurrentLang="
		+curLang+"\nlanguages="
		+langs+"\nlastSync=\n}\n"
		+ model.design.getComment());
}
function updTranslInfo(curLang,langs,datetime) {
	curComment = model.design.getComment();
	model.design.setComment(
		curComment.replace(gTranslInforegexp,
		"TRANSLATION{\ncurrentLang="
		+curLang+"\nlanguages="
		+langs+"\nlastSync="+datetime+"\n}"
		));
}

function buildRegExpTag(innertag,tag){
	return tag.replace('<tag>',innertag).replace(/\[/g,'\\[').replace(/\]/g,'\\]')
}

function textFromNote(tag,obj) {
    	note = obj.getNotes()
    	restart =  buildRegExpTag(tag, notesStartTag)
    	reend =  buildRegExpTag (tag, notesEndTag)
    	//throw new Error (restart+"*"+reend);
    	re = new RegExp(restart + '([^]*)' + reend)
    	ma = note.match(re)
    	retval = ''
   	if (ma != null) {
   		retval = ma[1]
	}
	return retval
}

function textToNote(tag,obj,value) {
    	note = obj.getNotes()
    	restart =  buildRegExpTag(tag, notesStartTag)
    	reend =  buildRegExpTag (tag, notesEndTag)
    	re = new RegExp(restart + '([^]*)' + reend)
    	ma = note.match(re)
    	newentry = notesStartTag.replace('<tag>',tag) 
    			+ value 
    			+ notesEndTag.replace('<tag>',tag)
    			+ "\n"
   	if (ma == null) {
   		note = note + newentry
	} else {
		note = note.replace(re , newentry)
	}

	obj.setNotes(note)
}

function init(){
	lmatchTransl = getTranslInfo();
	if (lmatchTransl == null) {
		throw new Error("***TRANSLATION not installed***");
	}
	ltext= String(lmatchTransl).match(/currentLang=[A-Z]{2}/);
	lparts = String(ltext).split("=");
	//log("curLang="+lparts[1]);
     gCurrentLang = lparts[1];

	ltext= String(lmatchTransl).match(/languages=[A-Z,]*/);
	lparts = String(ltext).split("=");
     //log("languages="+lparts[1]);
     gLanguages = lparts[1]
}

function doProperty(lang,propName,obj,value){
	lpropName = lang + propName;
	if (lang == gCurrentLang) {
		// die Werte kommen aus dem Objekt, überschreibe immer
		obj.setProperty(lpropName
		      	 	,value)
	} else {
	   	ldest = obj.getProperty(lpropName);
	   	// überschreibe nur, wenn das Ziel leer ist
		if (ldest == '')  {
		 	// bei leerem sourceWert setze keinen Prefix
			if (value != '') {
				lprefix =  '*'+gCurrentLang+'* '
				obj.setProperty(lpropName,lprefix+value)
			} 
			
		}
	}
}

function doNote(lang,tag,obj,value) {
	if (lang == gCurrentLang) {
		// die Werte kommen aus dem Objekt, überschreibe immer
		textToNote(lang+tag,obj,value)
	} else {
	   	ldest = textFromNote(lang+tag,obj);
	   	// überschreibe nur, wenn das Ziel leer ist
		if (ldest == '')  {
		 	// bei leerem sourceWert setze keinen Prefix
			if (value != '') {
				lprefix =  '*'+gCurrentLang+'* '
				textToNote(lang+tag,obj,lprefix+value)
			} 
			
		}
	}
	
}			
	
function fuellUDP(){
	entities = model.getEntitySet().toArray();
	langs = gLanguages.split(',');
	for (ie=0;ie<entities.length;ie++) { //
		ent = entities[ie];
		// noch nicht initialisiert
		message("for fuelludp")
		for (il=0;il<langs.length;il++) {
			doProperty(langs[il],"_ENTI_NAME",ent,ent.getName())
//			doProperty(langs[il],"_ENTI_COMMENT",ent,ent.getComment())
			doNote(langs[il],"_ENTI_COMMENT",ent,ent.getComment())
			doProperty(langs[il],"_ENTI_SYNONYM",ent,ent.getSynonym())
		}
	
		attrs = ent.getAttributes().toArray();
		//log("Attrs="+attrs.length)
		for (ia=0;ia<attrs.length;ia++) { //
			attr = attrs[ia];
			//log(attr.getName(),attr.getComment());
			for (il=0;il<langs.length;il++) {
				doProperty(langs[il],"_ATTR_NAME",attr,attr.getName());
//				doProperty(langs[il],"_ATTR_COMMENT",attr,attr.getComment());			
				doNote(langs[il],"_ATTR_COMMENT",attr,attr.getComment());			
		}
		}
		ent.setDirty(true);
	}
	relationships = model.getRelationSet().toArray();
	for (ir=0;ir<relationships.length;ir++) { //
		rel = relationships[ir];
		sourcename = rel.getNameOnSource()
		targetname = rel.getNameOnTarget()
		langs = gLanguages.split(',');
		for (il=0;il<langs.length;il++) {
			//if (i > 0) throw new Error ("\n"+langs[i]+"_RELA_TEXT_FROM"+"\n"+langs.length);
			doProperty(langs[il],"_RELA_TEXT_FROM",rel,sourcename)
			doProperty(langs[il],"_RELA_TEXT_TO",rel,targetname)	
		}
		rel.setDirty(true);
	}
}

function setProperties(lang){
	entities = model.getEntitySet().toArray();
	for (ie=0;ie<entities.length;ie++) { //
		ent = entities[ie];
		ent.setName(ent.getProperty(lang+"_ENTI_NAME"));
//		ent.setComment(ent.getProperty(lang+"_ENTI_COMMENT"));
		ent.setComment(textFromNote(lang+"_ENTI_COMMENT",ent))
		ent.setSynonym(ent.getProperty(lang+"_ENTI_SYNONYM"));
		
		attrs = ent.getAttributes().toArray();
		//log("Attrs="+attrs.length)
		for (ia=0;ia<attrs.length;ia++) { //
			attr = attrs[ia];
			//log(attr.getName(),attr.getComment());
			attr.setName(attr.getProperty(lang+"_ATTR_NAME"));
//			attr.setComment(attr.getProperty(lang+"_ATTR_COMMENT"));			
			attr.setComment(textFromNote(lang+"_ATTR_COMMENT",attr))
		}		
		ent.setDirty(true);
	}
	relationships = model.getRelationSet().toArray();
	for (ir=0;ir<relationships.length;ir++) { 
		rel = relationships[ir];
		rel.setNameOnSource(rel.getProperty(lang+"_RELA_TEXT_FROM"));
		rel.setNameOnTarget(rel.getProperty(lang+"_RELA_TEXT_TO"));
		rel.setDirty(true);
	}
}

function curDateStr(){
	var currentdate = new Date();
	return  currentdate.getDate() 
	+ "/" + (currentdate.getMonth()+1) 
	+ "/" + currentdate.getFullYear() + "  " 
	+ currentdate.getHours() + ":" 
	+ currentdate.getMinutes() + ":" + currentdate.getSeconds();
}
function initTranslation() {
	lmatchTransl= getTranslInfo()
	if (lmatchTransl == null) {
		setTranslInfo(gCurrentLang
		,gLanguages);		
	} else {
		throw new Error("***TRANSLATION already installed***");
	}
	//hier gibt es einen Eintrag für die Translation
	init();
	// fülle die UDP für die Übersetzungen
	fuellUDP();
	updTranslInfo(gCurrentLang, gLanguages
				,curDateStr());
	model.design.setDirty(true);
}


function changeLang(pfromLang,ptoLang){
	//log("3:"+gLanguages);
//	log("change from " + pfromLang + " to " + ptoLang);
	fuellUDP();
	setProperties(ptoLang);
	gCurrentLang = ptoLang;
	updTranslInfo(gCurrentLang, gLanguages
				,curDateStr());	
	model.design.setDirty(true);
}

function change2DE() {
	init();
	if (gCurrentLang != 'DE') {
		changeLang(gCurrentLang,'DE');
		message ("Modell auf DE übersetzt. Bitte Labels aus- und wieder einblenden."
		        ,plainMessage);
	}
}
	
function change2EN() {
	init();
	if (gCurrentLang != 'EN') {
		changeLang(gCurrentLang,'EN');
		message ("Model translated to EN. Please hide and re-display labels."
		        ,plainMessage);
	}
}]]>
		</script>
	</scr>
	<scr id="EBC813F0-0BAC-1802-1DB2-48F2CA8113A3" name="TRANSL_change2EN" object="logical" engine="Oracle Nashorn" type="" var="model" library="translation" method="change2EN" purpose="transformation" >
		<script>
			<![CDATA[//scripts to handle translation of modelx
var gLogNote = "LOG";
var gCurrentLang = "DE";
var gLanguages = "DE,EN";
var gTranslInforegexp =/TRANSLATION\{[^}]*\}/;
var logInitialized = false;
var notesStartTag = '[<tag>[\n';
var notesEndTag = '\n]<tag>]';
var plainMessage = 1;
var errorMessage = 0;

function fullmessage(msg,type) {
	Java.type("javax.swing.JOptionPane").showMessageDialog(null
	, msg,'ODM-Translation', type);
}

function error(msg) {
	fullmessage(msg,errorMessage);
	}
function message(msg) {
	fullmessage(msg,plainMessage);
	}

function initLog(){
	if (! logInitialized){
		var lNote = model.getNoteSet().getByName(gLogNote);
     	if (lNote == null) {
    	    		lNote = model.createNote()
	    		lNote.setName(gLogNote);
     	}
     	lNote.setComment("");
     	logInitialized = true;
	}
}
function log(plog){
	initLog();
	var lNote = model.getNoteSet().getByName(gLogNote);
     lNote.setComment(lNote.getComment()+"\n"+plog);
}

function getTranslInfo(){
	var lDesignComment = model.design.getComment();
     lmatchTransl= lDesignComment.match(gTranslInforegexp);
     return lmatchTransl;    
}

function setTranslInfo(curLang,langs) {
	model.design.setComment(
		"TRANSLATION{\ncurrentLang="
		+curLang+"\nlanguages="
		+langs+"\nlastSync=\n}\n"
		+ model.design.getComment());
}
function updTranslInfo(curLang,langs,datetime) {
	curComment = model.design.getComment();
	model.design.setComment(
		curComment.replace(gTranslInforegexp,
		"TRANSLATION{\ncurrentLang="
		+curLang+"\nlanguages="
		+langs+"\nlastSync="+datetime+"\n}"
		));
}

function buildRegExpTag(innertag,tag){
	return tag.replace('<tag>',innertag).replace(/\[/g,'\\[').replace(/\]/g,'\\]')
}

function textFromNote(tag,obj) {
    	note = obj.getNotes()
    	restart =  buildRegExpTag(tag, notesStartTag)
    	reend =  buildRegExpTag (tag, notesEndTag)
    	//throw new Error (restart+"*"+reend);
    	re = new RegExp(restart + '([^]*)' + reend)
    	ma = note.match(re)
    	retval = ''
   	if (ma != null) {
   		retval = ma[1]
	}
	return retval
}

function textToNote(tag,obj,value) {
    	note = obj.getNotes()
    	restart =  buildRegExpTag(tag, notesStartTag)
    	reend =  buildRegExpTag (tag, notesEndTag)
    	re = new RegExp(restart + '([^]*)' + reend)
    	ma = note.match(re)
    	newentry = notesStartTag.replace('<tag>',tag) 
    			+ value 
    			+ notesEndTag.replace('<tag>',tag)
    			+ "\n"
   	if (ma == null) {
   		note = note + newentry
	} else {
		note = note.replace(re , newentry)
	}

	obj.setNotes(note)
}

function init(){
	lmatchTransl = getTranslInfo();
	if (lmatchTransl == null) {
		throw new Error("***TRANSLATION not installed***");
	}
	ltext= String(lmatchTransl).match(/currentLang=[A-Z]{2}/);
	lparts = String(ltext).split("=");
	//log("curLang="+lparts[1]);
     gCurrentLang = lparts[1];

	ltext= String(lmatchTransl).match(/languages=[A-Z,]*/);
	lparts = String(ltext).split("=");
     //log("languages="+lparts[1]);
     gLanguages = lparts[1]
}

function doProperty(lang,propName,obj,value){
	lpropName = lang + propName;
	if (lang == gCurrentLang) {
		// die Werte kommen aus dem Objekt, überschreibe immer
		obj.setProperty(lpropName
		      	 	,value)
	} else {
	   	ldest = obj.getProperty(lpropName);
	   	// überschreibe nur, wenn das Ziel leer ist
		if (ldest == '')  {
		 	// bei leerem sourceWert setze keinen Prefix
			if (value != '') {
				lprefix =  '*'+gCurrentLang+'* '
				obj.setProperty(lpropName,lprefix+value)
			} 
			
		}
	}
}

function doNote(lang,tag,obj,value) {
	if (lang == gCurrentLang) {
		// die Werte kommen aus dem Objekt, überschreibe immer
		textToNote(lang+tag,obj,value)
	} else {
	   	ldest = textFromNote(lang+tag,obj);
	   	// überschreibe nur, wenn das Ziel leer ist
		if (ldest == '')  {
		 	// bei leerem sourceWert setze keinen Prefix
			if (value != '') {
				lprefix =  '*'+gCurrentLang+'* '
				textToNote(lang+tag,obj,lprefix+value)
			} 
			
		}
	}
	
}			
	
function fuellUDP(){
	entities = model.getEntitySet().toArray();
	for (i=0;i<entities.length;i++) { //
		ent = entities[i];
		// noch nicht initialisiert
		langs = gLanguages.split(',');
		for (i=0;i<langs.length;i++) {
			doProperty(langs[i],"_ENTI_NAME",ent,ent.getName())
//			doProperty(langs[i],"_ENTI_COMMENT",ent,ent.getComment())
			doNote(langs[i],"DE_ENTI_COMMENT",ent,ent.getComment())
			doProperty(langs[i],"_ENTI_SYNONYM",ent,ent.getSynonym())
		}
	
		attrs = ent.getAttributes().toArray();
		//log("Attrs="+attrs.length)
		for (j=0;j<attrs.length;j++) { //
			attr = attrs[j];
			//log(attr.getName(),attr.getComment());
			for (i=0;i<langs.length;i++) {
				doProperty(langs[i],"_ATTR_NAME",attr,attr.getName());
//				doProperty(langs[i],"_ATTR_COMMENT",attr,attr.getComment());			
				doNote(langs[i],"_ATTR_COMMENT",attr,attr.getComment());			
		}
		}
		ent.setDirty(true);
	}
	relationships = model.getRelationSet().toArray();
	for (r=0;r<relationships.length;r++) { //
		rel = relationships[r];
		sourcename = rel.getNameOnSource()
		targetname = rel.getNameOnTarget()
		langs = gLanguages.split(',');
		for (i=0;i<langs.length;i++) {
			//if (i > 0) throw new Error ("\n"+langs[i]+"_RELA_TEXT_FROM"+"\n"+langs.length);
			doProperty(langs[i],"_RELA_TEXT_FROM",rel,sourcename)
			doProperty(langs[i],"_RELA_TEXT_TO",rel,targetname)	
		}
		rel.setDirty(true);
	}
}

function setProperties(lang){
	entities = model.getEntitySet().toArray();
	for (i=0;i<entities.length;i++) { //
		ent = entities[i];
		ent.setName(ent.getProperty(lang+"_ENTI_NAME"));
//		ent.setComment(ent.getProperty(lang+"_ENTI_COMMENT"));
		ent.setComment(textFromNote(lang+"_ENTI_COMMENT",ent))
		ent.setSynonym(ent.getProperty(lang+"_ENTI_SYNONYM"));
		
		attrs = ent.getAttributes().toArray();
		//log("Attrs="+attrs.length)
		for (j=0;j<attrs.length;j++) { //
			attr = attrs[j];
			//log(attr.getName(),attr.getComment());
			attr.setName(attr.getProperty(lang+"_ATTR_NAME"));
//			attr.setComment(attr.getProperty(lang+"_ATTR_COMMENT"));			
			attr.setComment(textFromNote(lang+"_ATTR_COMMENT",attr))
		}		
		ent.setDirty(true);
	}
	relationships = model.getRelationSet().toArray();
	for (r=0;r<relationships.length;r++) { 
		rel = relationships[r];
		rel.setNameOnSource(rel.getProperty(lang+"_RELA_TEXT_FROM"));
		rel.setNameOnTarget(rel.getProperty(lang+"_RELA_TEXT_TO"));
		rel.setDirty(true);
	}
}

function curDateStr(){
	var currentdate = new Date();
	return  currentdate.getDate() 
	+ "/" + (currentdate.getMonth()+1) 
	+ "/" + currentdate.getFullYear() + "  " 
	+ currentdate.getHours() + ":" 
	+ currentdate.getMinutes() + ":" + currentdate.getSeconds();
}
function initTranslation() {
	lmatchTransl= getTranslInfo()
	if (lmatchTransl == null) {
		setTranslInfo(gCurrentLang
		,gLanguages);		
	} else {
		throw new Error("***TRANSLATION already installed***");
	}
	//hier gibt es einen Eintrag für die Translation
	init();
	// fülle die UDP für die Übersetzungen
	fuellUDP();
	updTranslInfo(gCurrentLang, gLanguages
				,curDateStr());
	model.design.setDirty(true);
}


function changeLang(pfromLang,ptoLang){
	//log("3:"+gLanguages);
//	log("change from " + pfromLang + " to " + ptoLang);
	message("vor fuellu;dp");
	fuellUDP();
	message("vor setProp");
	setProperties(ptoLang);
	gCurrentLang = ptoLang;
	message("vor udpTrans");
	updTranslInfo(gCurrentLang, gLanguages
				,curDateStr());	
	model.design.setDirty(true);
}

function change2DE() {
	init();
	if (gCurrentLang != 'DE') {
		changeLang(gCurrentLang,'DE');
		message ("Modell auf DE übersetzt. Bitte Labels aus- und wieder einblenden."
		        ,plainMessage);
	}
}
	
function change2EN() {
	init();
	if (gCurrentLang != 'EN') {
		changeLang(gCurrentLang,'EN');
		message ("Model translated to EN. Please hide and re-display labels."
		        ,plainMessage);
	}
}]]>
		</script>
	</scr>
	<scr id="178EEC33-F1CD-15CE-69CF-2651B9BD882F" name="TestJavascript" object="logical" engine="Oracle Nashorn" type="" var="model" library="" method="" purpose="transformation" >
		<script>
			<![CDATA[ent = model.getEntitySet().getByName("Modellelement");
note = ent.getNotes()

var notesStartTag = '[<lang>[\n';
var notesEndTag = '\n]<lang>]';
var lang = 'EN'
Java.type("javax.swing.JOptionPane").showMessageDialog(null, "Hello Message"
				,"ODM-Translation",1);
	   //PLAIN_MESSAGE = -1 oder 1 mit ODM-Symbol
		//JOptionPane.ERROR_MESSAGE  = 0
model.getAppView().logMessage("message");
     restart =  notesStartTag.replace('<lang>',lang).replace(/\[/g,'\\[')
    	reend =  notesEndTag.replace('<lang>',lang).replace(/\]/g,'\\]')
    	//throw new Error (restart+"*"+reend);
    	re = new RegExp(restart + '([^]*)' + reend)
    	ma = note.match(re)
   	retval = 'nichts'
   	if (ma != null) {
   		retval = ma[1]
	}
	throw new Error ("**"+retval+"**");
//detext = no.match(/\[DE\[(.*)\]DE\]/)
detext = no.match(/\[DE\[\n([^]*)\n\]DE\]/)
entext = no.match(/\[EN\[\n([^]*)\n\]EN\]/)
throw new Error ("\n"+entext.length);
newen = '** This is english text\n\nseveral<> lines'
var re = /\[EN\[([^]*)\]EN\]/
no = no.replace(re, "\[EN\[\n" + newen + "\n]EN]")
throw new Error ("\n"+no+"\n");]]>
		</script>
	</scr>

	<lib id="3648E159-EDD2-ECD1-C014-A11CB3E804A5" name="translation" engine="Oracle Nashorn" methods="initTranslation,change2DE,change2EN" >
		<script>
			<![CDATA[
			//scripts to handle translation of model
			var gLogNote = "LOG";
			var gCurrentLang = "DE";
			var gLanguages = "DE,EN";
			var gTranslInforegexp =/TRANSLATION\{[^}]*\}/;
			var logInitialized = false;
			var notesStartTag = '[<tag>[\n';
			var notesEndTag = '\n]<tag>]';
			var plainMessage = 1;
			var errorMessage = 0;

			function fullmessage(msg,type) {
				Java.type("javax.swing.JOptionPane").showMessageDialog(null
				, msg,'ODM-Translation', type);
			}

			function error(msg) {
				fullmessage(msg,errorMessage);
				}
			function message(msg) {
				fullmessage(msg,plainMessage);
				}

			function initLog(){
				if (! logInitialized){
					var lNote = model.getNoteSet().getByName(gLogNote);
			     	if (lNote == null) {
			    	    		lNote = model.createNote()
				    		lNote.setName(gLogNote);
			     	}
			     	lNote.setComment("");
			     	logInitialized = true;
				}
			}
			function log(plog){
				initLog();
				var lNote = model.getNoteSet().getByName(gLogNote);
			     lNote.setComment(lNote.getComment()+"\n"+plog);
			}

			function getTranslInfo(){
				var lDesignComment = model.design.getComment();
			     lmatchTransl= lDesignComment.match(gTranslInforegexp);
			     return lmatchTransl;    
			}

			function setTranslInfo(curLang,langs) {
				model.design.setComment(
					"TRANSLATION{\ncurrentLang="
					+curLang+"\nlanguages="
					+langs+"\nlastSync=\n}\n"
					+ model.design.getComment());
			}
			function updTranslInfo(curLang,langs,datetime) {
				curComment = model.design.getComment();
				model.design.setComment(
					curComment.replace(gTranslInforegexp,
					"TRANSLATION{\ncurrentLang="
					+curLang+"\nlanguages="
					+langs+"\nlastSync="+datetime+"\n}"
					));
			}

			function buildRegExpTag(innertag,tag){
				return tag.replace('<tag>',innertag).replace(/\[/g,'\\[').replace(/\]/g,'\\]')
			}

			function textFromNote(tag,obj) {
			    	note = obj.getNotes()
			    	restart =  buildRegExpTag(tag, notesStartTag)
			    	reend =  buildRegExpTag (tag, notesEndTag)
			    	//throw new Error (restart+"*"+reend);
			    	re = new RegExp(restart + '([^]*)' + reend)
			    	ma = note.match(re)
			    	retval = ''
			   	if (ma != null) {
			   		retval = ma[1]
				}
				return retval
			}

			function textToNote(tag,obj,value) {
			    	note = obj.getNotes()
			    	restart =  buildRegExpTag(tag, notesStartTag)
			    	reend =  buildRegExpTag (tag, notesEndTag)
			    	re = new RegExp(restart + '([^]*)' + reend)
			    	ma = note.match(re)
			    	newentry = notesStartTag.replace('<tag>',tag) 
			    			+ value 
			    			+ notesEndTag.replace('<tag>',tag)
			    			+ "\n"
			   	if (ma == null) {
			   		note = note + newentry
				} else {
					note = note.replace(re , newentry)
				}

				obj.setNotes(note)
			}

			function init(){
				lmatchTransl = getTranslInfo();
				if (lmatchTransl == null) {
					throw new Error("***TRANSLATION not installed***");
				}
				ltext= String(lmatchTransl).match(/currentLang=[A-Z]{2}/);
				lparts = String(ltext).split("=");
				//log("curLang="+lparts[1]);
			     gCurrentLang = lparts[1];

				ltext= String(lmatchTransl).match(/languages=[A-Z,]*/);
				lparts = String(ltext).split("=");
			     //log("languages="+lparts[1]);
			     gLanguages = lparts[1]
			}

			function doProperty(lang,propName,obj,value){
				lpropName = lang + propName;
				if (lang == gCurrentLang) {
					// die Werte kommen aus dem Objekt, überschreibe immer
					obj.setProperty(lpropName
					      	 	,value)
				} else {
				   	ldest = obj.getProperty(lpropName);
				   	// überschreibe nur, wenn das Ziel leer ist
					if (ldest == '')  {
					 	// bei leerem sourceWert setze keinen Prefix
						if (value != '') {
							lprefix =  '*'+gCurrentLang+'* '
							obj.setProperty(lpropName,lprefix+value)
						} 
			
					}
				}
			}

			function doNote(lang,tag,obj,value) {
				if (lang == gCurrentLang) {
					// die Werte kommen aus dem Objekt, überschreibe immer
					textToNote(lang+tag,obj,value)
				} else {
				   	ldest = textFromNote(lang+tag,obj);
				   	// überschreibe nur, wenn das Ziel leer ist
					if (ldest == '')  {
					 	// bei leerem sourceWert setze keinen Prefix
						if (value != '') {
							lprefix =  '*'+gCurrentLang+'* '
							textToNote(lang+tag,obj,lprefix+value)
						} 
			
					}
				}
	
			}			
	
			function fuellUDP(){
				entities = model.getEntitySet().toArray();
				langs = gLanguages.split(',');
				for (ie=0;ie<entities.length;ie++) { //
					ent = entities[ie];
					// noch nicht initialisiert
					message("for fuelludp")
					for (il=0;il<langs.length;il++) {
						doProperty(langs[il],"_ENTI_NAME",ent,ent.getName())
			//			doProperty(langs[il],"_ENTI_COMMENT",ent,ent.getComment())
						doNote(langs[il],"_ENTI_COMMENT",ent,ent.getComment())
						doProperty(langs[il],"_ENTI_SYNONYM",ent,ent.getSynonym())
					}
	
					attrs = ent.getAttributes().toArray();
					//log("Attrs="+attrs.length)
					for (ia=0;ia<attrs.length;ia++) { //
						attr = attrs[ia];
						//log(attr.getName(),attr.getComment());
						for (il=0;il<langs.length;il++) {
							doProperty(langs[il],"_ATTR_NAME",attr,attr.getName());
			//				doProperty(langs[il],"_ATTR_COMMENT",attr,attr.getComment());			
							doNote(langs[il],"_ATTR_COMMENT",attr,attr.getComment());			
					}
					}
					ent.setDirty(true);
				}
				relationships = model.getRelationSet().toArray();
				for (ir=0;ir<relationships.length;ir++) { //
					rel = relationships[ir];
					sourcename = rel.getNameOnSource()
					targetname = rel.getNameOnTarget()
					langs = gLanguages.split(',');
					for (il=0;il<langs.length;il++) {
						//if (i > 0) throw new Error ("\n"+langs[i]+"_RELA_TEXT_FROM"+"\n"+langs.length);
						doProperty(langs[il],"_RELA_TEXT_FROM",rel,sourcename)
						doProperty(langs[il],"_RELA_TEXT_TO",rel,targetname)	
					}
					rel.setDirty(true);
				}
			}

			function setProperties(lang){
				entities = model.getEntitySet().toArray();
				for (ie=0;ie<entities.length;ie++) { //
					ent = entities[ie];
					ent.setName(ent.getProperty(lang+"_ENTI_NAME"));
			//		ent.setComment(ent.getProperty(lang+"_ENTI_COMMENT"));
					ent.setComment(textFromNote(lang+"_ENTI_COMMENT",ent))
					ent.setSynonym(ent.getProperty(lang+"_ENTI_SYNONYM"));
		
					attrs = ent.getAttributes().toArray();
					//log("Attrs="+attrs.length)
					for (ia=0;ia<attrs.length;ia++) { //
						attr = attrs[ia];
						//log(attr.getName(),attr.getComment());
						attr.setName(attr.getProperty(lang+"_ATTR_NAME"));
			//			attr.setComment(attr.getProperty(lang+"_ATTR_COMMENT"));			
						attr.setComment(textFromNote(lang+"_ATTR_COMMENT",attr))
					}		
					ent.setDirty(true);
				}
				relationships = model.getRelationSet().toArray();
				for (ir=0;ir<relationships.length;ir++) { 
					rel = relationships[ir];
					rel.setNameOnSource(rel.getProperty(lang+"_RELA_TEXT_FROM"));
					rel.setNameOnTarget(rel.getProperty(lang+"_RELA_TEXT_TO"));
					rel.setDirty(true);
				}
			}

			function curDateStr(){
				var currentdate = new Date();
				return  currentdate.getDate() 
				+ "/" + (currentdate.getMonth()+1) 
				+ "/" + currentdate.getFullYear() + "  " 
				+ currentdate.getHours() + ":" 
				+ currentdate.getMinutes() + ":" + currentdate.getSeconds();
			}
			function initTranslation() {
				lmatchTransl= getTranslInfo()
				if (lmatchTransl == null) {
					setTranslInfo(gCurrentLang
					,gLanguages);		
				} else {
					throw new Error("***TRANSLATION already installed***");
				}
				//hier gibt es einen Eintrag für die Translation
				init();
				// fülle die UDP für die Übersetzungen
				fuellUDP();
				updTranslInfo(gCurrentLang, gLanguages
							,curDateStr());
				model.design.setDirty(true);
			}


			function changeLang(pfromLang,ptoLang){
				//log("3:"+gLanguages);
			//	log("change from " + pfromLang + " to " + ptoLang);
				fuellUDP();
				setProperties(ptoLang);
				gCurrentLang = ptoLang;
				updTranslInfo(gCurrentLang, gLanguages
							,curDateStr());	
				model.design.setDirty(true);
			}

			function change2DE() {
				init();
				if (gCurrentLang != 'DE') {
					changeLang(gCurrentLang,'DE');
					message ("Modell auf DE übersetzt. Bitte Labels aus- und wieder einblenden."
					        ,plainMessage);
				}
			}
	
			function change2EN() {
				init();
				if (gCurrentLang != 'EN') {
					changeLang(gCurrentLang,'EN');
					message ("Model translated to EN. Please hide and re-display labels."
					        ,plainMessage);
				}
			}
			]]>
		</script>
	</lib>
	<lib id="3AE4DC06-F989-16D9-C5B2-6E62EDBC8D0B" name="searchhandling" engine="Oracle Nashorn" methods="subviewfromsearch" >
		<script>
			<![CDATA[// div. functions to handle complex logical manipulations
function subviewfromsearch() {
objs = model.getLastSearchResult().toArray();
var dp;
var sv;
//create subview and add tables to it
for(var i = 0;i<objs.length;i++){
	obj = objs[i];
	// use "Entity" for entities and logical model
	if(obj.getObjectTypeName().equals("Table")){
		if(dp==null){
			dp = obj.getDesignPart();
			sv = dp.createDesignPartSubView();
			// uncomment next line if want to set name
		     //sv.setName("Name");
		     sv.getPlaceHolder().setVisible(true);
		}
		sv.addViewFor(obj);
	}
}
if(dp!=null){
 //add foreign keys if you want to
 for(var i = 0;i<objs.length;i++){
	obj = objs[i];
	// use "Entity" for entities and logical model
	if(obj.getObjectTypeName().equals("Table")){
		tv = obj.getFirstViewForDPV(sv);
		if(tv!=null){
			//for entities and logical model use 
			//tv.addTVRelations(); 
			tv.addTVFKRelations();
		}
	}
 }
 //add Arcs if you want to
 for(var i = 0;i<objs.length;i++){
	obj = objs[i];
	if(obj.getObjectTypeName().equals("Table")){
		tv = obj.getFirstViewForDPV(sv);
		if(tv!=null){
			tv.addArcs();
		}
	}
 }
 //arrange diagram
 sv.rearrangeNewDiagram();
}
}]]>
		</script>
	</lib>
	<lib id="287FBD38-4CE0-8E80-9D01-96BC381B80E9" name="relationalmodel" engine="Oracle Nashorn" methods="names2sqlcompatible,columnprefix,addtemplatecols,alltables" >
		<script>
			<![CDATA[//div. scripts to change tables after generation
function names2sqlcompatible(){
	// removes all non-sql-compatible characters from
	// all names
 var tables = model.getTableSet().toArray();
 for (var t = 0; t<tables.length;t++){
	table = tables[t];
	tabname = table.getName().replace("-","_");
	table.setName(tabname.toUpperCase());

 	columns = table.getElements();
     for (var i = 0; i < columns.length; i++) {
        column = columns[i];
        colname = column.getName().replace("-","_");
        column.setName(colname.toUpperCase());
 
 	keys = table.getKeys();
 	for (var i = 0; i < keys.length; i++) {
  		key = keys[i];
    		if(!key.isFK()){
     		kname = key.getName().replace("-","_");
     		key.setName(kname.toUpperCase());
   		}else{
	     	kname = key.getFKAssociation().getName().replace("-","_");
     		key.getFKAssociation().setName(kname.toUpperCase());
     		key.getFKAssociation().setDirty(true);
   		}
 	table.setDirty(true);
 }
} 

fumction gettempaltetable(){
 templtable = model.getTableSet().getById(model.getTemplateTableID())
 return templtable
}

function tablecolprefix(table) {
	return table.getAbbreviation().toCpperCase() + "_";
}

function 	columnprefix1table(table) {
 // sets the column Prefix to table abbrev for 1 table
 	abbr = tablecolprefix(table);
 	if(!"_".equals(abbr)){
     columns = table.getElements();
     for (var i = 0; i < columns.length; i++) {
        column = columns[i];
        cname = column.getName().toUpperCase;
        if(!cname.startsWith(abbr)){
        	column.setName(abbr+cname);
        }
     }
}

function columnprefix() {
 var temptable = gettemplatetable();
 var tables = model.getTableSet().toArray();
 for (var t = 0; t<tables.length;t++){
 	if(!table.getId() == templtable.getId()) {
	 	table = tables[t];
 		columnprefix1table(table)
     	table.setDirty(true)
 	}	 
 }
}

function addtemplatecols() {
 // columns are found by column name or ABBREV_colname
//prop var colproptemplcolid = "coltemplID";
 var templtable =  gettempaltetable();
 if(template!=null){
    tcolumns = template.getElements();
    tables = model.getTableSet().toArray();
    for (var t = 0; t<tables.length;t++){
     table = tables[t];
     // compare id to exclude templ table
     if(!table.getId() == templtable.getId()) {
         for (var i = 0; i < tcolumns.length; i++) {
	     	column = tcolumns[i];
            col = table.getElementByName(column.getName());
            if(col==null){
			// evtl. schon mit prefix vorhanden
              col = table.getElementByName(tablecolprefix(table) + column.getName());
            }
//prop            if(col==null){
//prop             col = table.getColumnByProperty(colproptemplcolid,column.getObjectID());
//prop            }
            if(col==null){
             col = table.createColumn();
            }
            column.copy(col);
            //set property after copy otherwise it will be cleared by copy
//prop            col.setProperty(colproptemplcolid,column.getObjectID());
 	   	    columnprefix1table(table)
            table.setDirty(true);
         }
     }
 }
}

function alltables(){
	// does all changes to all tables
	names2sqlcompatible;
	columnprefix();
	addtemplatecols();
}]]>
		</script>
	</lib>


	<ddl_transformation_script_set id="B4E65BCB-CFF5-1302-E0BC-0C329BF9890E" name="Journal tables" description="" active="true">
	</ddl_transformation_script_set>
	<ddl_transformation_script_set id="86649BAE-547E-99D5-8B00-54B3BB9BB6BB" name="Empty script" description="" active="false">
	</ddl_transformation_script_set>
</dr_custom_scripts>